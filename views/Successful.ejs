<!DOCTYPE html>
<html>
<head>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Success Page</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 0;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .icon {
      font-size: 100px;
      color: #007bff;
    }

    .success-text {
      font-size: 24px;
      color: #007bff;
      margin-bottom: 20px;
    }

    .description {
      font-size: 20px;
      text-align: center;
      max-width: 80%;
    }

    .download-link {
      display: block;
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      font-size: 18px;
      transition: background-color 0.3s ease;
    }

    .download-link:hover {
      background-color: #0056b3;
    }
    .copy-icon {
      margin-left: 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="container">
    <i class="fas fa-check-circle icon"></i>
    <h2 class="success-text"> <%= message %></h2>
    <div class="description">
<span id="download-link-text">   <%= Link %></span>
      <i class="far fa-copy copy-icon" onclick="copyToClipboard()"></i>
    </div>
    <a href="/" class="download-link">Back to Homepage</a>
  </div>
  <script>
    function copyToClipboard() {
      const textToCopy = document.getElementById('download-link-text').innerText;
      const tempInput = document.createElement('input');
      tempInput.value = textToCopy;
      document.body.appendChild(tempInput);
      tempInput.select();
      document.execCommand('copy');
      document.body.removeChild(tempInput);

      alert('Link copied to clipboard!');
    }
  </script>
</body>
</html>
